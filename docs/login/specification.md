# 🚀 Cassandra Framework
## Техническое задание: Страница Login (Авторизация)

### 📌 Обозначения

-   **`ElementName`** *(type)* — UI-элемент с указанием типа
-   *"Text content"* — отображаемый текст на интерфейсе
-   `code` — технические термины, классы, методы
-   `'/login.html'` *(url)* — адрес страницы (URL) для проверки навигации
-   ⚠️ *(decorative)* — декоративный элемент, не подлежит автоматизированному тестированию

## 1. 🎯 Назначение
Центральный экран аутентификации для доступа к системе оценки колонизации планет. Обеспечивает ввод учётных данных, клиентскую валидацию и передачу данных в сервис авторизации.

## 2. 🖥️ Элементы интерфейса

### 2.1 Основные элементы формы

-   **`Brand Title`** *(text)* — Заголовок "CASSANDRA" со стилизованным суффиксом. *"CASSAN"* отображается белым шрифтом, *"DRA"* имеет голубое свечение.

-   **`Subtitle`** *(text)* — Статичная подпись под заголовком. Текст: *"Access to Colonization Assessment System"*.

-   **`Auth Error Block`** *(alert)* — Блок ошибки авторизации. Отображается над формой при неуспешной аутентификации. Текст: *"⚠️ Invalid callsign or access code"*. По умолчанию скрыт.

-   **`Callsign`** *(input)* — Поле ввода позывного исследователя. Placeholder: *"Enter callsign"*. Максимальная длина: 100 символов. Допустимые символы: буквы (латиница) и цифры. Ввод пробелов и большинства спецсимволов блокируется или отсекается через JS."

    -   *Реализация валидации:* Клиентская проверка на уровне UI. Ограничение длины enforced через HTML-атрибут `maxlength`. Ввод кириллицы блокируется через JS.

-   **`Access Code`** *(input)* — Поле ввода ключа доступа. Placeholder: *"Enter access code"*. Максимальная длина: 30 символов. Допустимые символы: **буквы (латиница), цифры и нижнее подчеркивание (`_`)**.

    -   *Реализация валидации:* Клиентская проверка на уровне UI. Тип поля `type="password"`. Фильтрация невалидных символов enforced через JS (`replace(/[^a-zA-Z0-9_]/g, '')`) и атрибут `inputmode="text"`.

-   **`Toggle Password Button`** *(button)* — Кнопка переключения видимости ключа доступа. Расположена внутри поля `Access Code` справа. Содержит SVG-иконку глаза (открытый/закрытый). При нажатии переключает тип поля между `password` и `text`.

-   **`Establish Connection`** *(button)* — Кнопка подтверждения входа. Label: *"Establish Connection"*. Состояние: неактивна (`disabled`), пока поле `Callsign` не содержит ≥ 4 символов, а поле `Access Code` не содержит ≥ 4 символов.

-   **`Corner Navigation Container`** *(layout)* — Контейнер для вторичной навигации, зафиксированный в нижней части экрана по углам (`position: fixed`). Не мешает взаимодействи с основной формой.

-   **`Sign Up Corner Button`** *(button/link)* — Круглая навигационная кнопка в левом нижнем углу. Иконка: 📡. Label: *"Sign Up"*. Стиль идентичен кнопкам на Index page. При клике происходит переход на `signup.html`.

-   **`Restore Corner Button`** *(button/link)* — Круглая навигационная кнопка в правом нижнем углу. Иконка: 🔑. Label: *"Restore"*. Стиль идентичен кнопкам на Index page. При клике происходит переход на `access-restoration.html`.

### 2.2 Декоративные элементы ⚠️

> **Примечание:** Элементы данного раздела являются частью визуального оформления страницы и **не подлежат автоматизированному тестированию**. Они не имеют `data-wm-id` и не включаются в CAS-сценарии.

-   **`Satellite`** *(decorative/svg)* — Летающий спутник с солнечными панелями и антенной. Перемещается по экрану с отталкиванием от границ окна браузера (эффект "DVD-логотипа"). От спутника каждые ~2 секунды расходятся три радиоволны (анимированные SVG-круги), символизирующие поиск сигнала. Проходит за формой авторизации (z-index ниже карточки), создавая эффект "мёртвой зоны".

-   **`Stars Background`** *(decorative/css)* — Анимированный фон со звёздами и Млечным Путём.

### 2.3 Служебные элементы

-   **`Telemetry`** *(text)* — Строка состояния системы в футере. Отображается моноширинным шрифтом с мигающим курсором (`█`). 

    -   Состояние по умолчанию (до авторизации): *"> SYSTEM READY. AWAITING CONNECTION"* (синий цвет, rgb(77, 166, 255) / #4da6ff).

    -   Состояние после успешной авторизации: *"> CONNECTION ESTABLISHED. WELCOME, {ROLE} {FULL_NAME}"* (зелёный цвет,rgb(46, 204, 113) / #2ecc71).

    -   Состояние после неуспешной авторизации: *"> SYSTEM FAILURE. INVALID CREDENTIALS"* (красный цвет, rgb(231, 76, 60) / #e74c3c).

-   **`Footer Brand`** *(text)* — Копирайт в футере. Текст: *"Evknopia © 2026"*.

## 3. ✅ Правила валидации

-   **`Callsign`**: 
    -   Минимальная длина — 4 символа. 

    -   Поведение: Пока не введено минимум 4 символа, кнопка **`Establish Connection`** физически неактивна 
    (`disabled`). Ошибки под полем не отображаются.

-   **`Access Code`**: 
    -   Минимальная длина — 4 символа. 

    -   Поведение: Пока не введено минимум 4 символа, кнопка **`Establish Connection`** физически неактивна (`disabled`). Ошибки под полем не отображаются.

-   **Итог**: Валидация минимальной длины реализована исключительно через блокировку кнопки отправки. Пользователь не может отправить форму с невалидной длиной ни кликом мыши, ни клавишей `Enter`.

## 4. ⚙️ Логика работы

> ⚠️ **Важно: Контекст проекта**
> Данный проект является учебным и **не имеет серверной части (бэкенда)**. 
> Все процессы аутентификации, хранения пользователей и передачи данных эмулируются исключительно на стороне клиента (Frontend) с использованием `sessionStorage` (для хранения активной сессии currentUser), `localStorage` (для эмуляции базы данных `registeredUsers`) и локальных конфигурационных файлов (data.js).
> В реальном продакшн-проекте эти функции выполнялись бы на сервере.

-   Плейсхолдеры скрываются при наличии ввода и отображаются при пустом значении поля.

-   Состояние кнопки **`Establish Connection`** обновляется реактивно: она неактивна, пока `Callsign` < 4 символов или `Access Code` < 4 символов.

-   Ввод в поле **`Callsign`** фильтруется в реальном времени: блокируется ввод кириллицы (допускаются латиница, цифры и спецсимволы).

-   Ввод в поле **`Access Code`** фильтруется в реальном времени: допускаются только буквы латиницы, цифры и нижнее подчеркивание (`_`).

-   Аутентификация выполняется путем прямой проверки введенных данных с объектом `MOCK_USERS` из файла `data.js` (без обращения к внешнему API).

## 5. 🤖 Спецификация объекта MOCK_USERS: Логика аутентификации

-   **Источник данных:** Глобальный объект `MOCK_USERS`, объявленный в файле `data.js`.

-   **Назначение:** Эмуляция базы данных пользователей на стороне клиента. Не выполняет валидацию формата/длины — это ответственность UI.

-   **Структура данных:** Ключом объекта является позывной (например, `ORION`), а значением — объект с данными пользователя, содержащий поля `role`, а также позывной и ключ доступа (в data.js это `accessCode` и `fullName`, в user.json — `access_code` и `full_name`).

- **Эталонная структура данных (пример для AURORA):**

    - {
        - "callsign": "AURORA",
        - "fullName": "Aurora",
        - "role": "SPECIALIST",
        - "roleIcon": "🛰️",
        - "function": "Comms & Diagnostics",
        - "accessLevel": "2",
        - "accessCode": "COMET_42",
        - "id": "884-2A"
    - }

-   **Конфигурация валидных данных (для демонстрации):**

    -   Аккаунт 1: Позывной `ORION`, Ключ `NEBULA_7`, Роль `Commander`
    -   Аккаунт 2: Позывной `AURORA`, Ключ `COMET_42`, Роль `Specialist`

-   **Порядок поиска пользователя:** 

    - Первый приоритет: Система ищет позывной в `localStorage` по ключу `restoredUsers` (база пользователей, успешно прошедших процедуру восстановления).

    - Второй приоритет: Если пользователь не найден в восстановленных, система ищет позывной в `localStorage` по ключу `registeredUsers` (динамическая база зарегистрированных пользователей, например `NOVA`).

    - Третий приоритет: Если пользователь не найден нигде в `localStorage`, система ищет его в глобальном объекте `MOCK_USERS` из файла `data.js` (статические системные аккаунты `AURORA`, `ORION`).


- 💡 **Специфические операторы:**

- **Оператор NOVA:**

    - Создается через регистрацию `signup.html`.

    - {
        - "callsign": "NOVA",
        - "fullName": "Nova",
        - "role": "ENGINEER",
        - "roleIcon": "⚙️",
        - "function": "Systems Engineering",
        - "accessLevel": "3",
        - "accessCode": "QUASAR_5",
        - "id": "512-3A"
    - }

- **Оператор KNOPA:**

    - Предсуществующий, восстанавливается только через `access-restoration.html`.

    - {
        - "callsign": "KNOPA",
        - "fullName": "Knopa",
        - "role": "PILOT",
        - "roleIcon": "✈️",
        - "function": "Flight Operations",
        - "accessLevel": "1",
        - "accessCode": "AERO_99",
        - "id": "769-1A"
    - }

-   **Логика сравнения:** Возвращает `True` только если позывной найден (в любом из трёх источников) и введённый ключ доступа точно совпадает с сохранённым `accessCode`. Во всех остальных случаях — `False`. Сообщение об ошибке универсально и не раскрывает, на каком этапе или в какой базе произошла ошибка (принцип `security by obscurity`).

## 6. 🔄 Обработка результатов аутентификации

-   **Успешная аутентификация:**

    -   **Условие:** Входные данные совпали с валидной парой из конфигурации.

    -   **Действие UI (Пошаговый UX-флоу):**

        -   Мгновенный отклик: При клике кнопка физически «проваливается» (визуальный эффект нажатия), меняет текст на `CONNECTING...` и начинает пульсировать. Повторные клики блокируются.

        -   Имитация задержки сети (2 секунды): В течение этого времени текст `Telemetry` остаётся в исходном состоянии (синий `> SYSTEM READY...`), создавая эффект «полёта сигнала».

        -   Успешное соединение: Ровно через 2 секунды текст `Telemetry` меняется на зелёный: `> CONNECTION ESTABLISHED. WELCOME, {ROLE} {FULL_NAME}`.

        -   Редирект: Данные пользователя сохраняются в `sessionStorage` (ключ `currentUser`). Спустя ещё 2.0 секунды (пауза для прочтения приветствия) происходит автоматический переход на главную страницу (dashboard.html).

        -   Данные пользователя сохраняются в `sessionStorage` (ключ `currentUser`) для использования на последующих страницах в рамках текущей вкладки.

-   **Неуспешная аутентификация:**

    -   **Условие:** Любая комбинация позывного и ключа доступа, кроме валидной пары из конфигурации.

    -   **Действие UI (Пошаговый UX-флоу):**

        -   Мгновенный отклик: При клике кнопка физически «проваливается», меняет текст на `CONNECTING...` и начинает пульсировать. Текст `Telemetry` остаётся в исходном синем состоянии (`> SYSTEM READY...`).

        -   Имитация задержки сети (2 секунды): Система «обрабатывает» запрос. В это время ошибки на экране нет.

        -   Появление ошибки: Ровно через 2 секунды сверху отображается блок `Auth Error Block` с текстом `️ Invalid callsign or access code`. Текст `Telemetry` меняется на красный: `> SYSTEM FAILURE. INVALID CREDENTIALS`. Кнопка возвращается в активное (яркое) состояние.

        -   Цикличность при повторной попытке: При повторном нажатии на кнопку блок ошибки мгновенно исчезает, телеметрия сбрасывается в исходный синий цвет (`> SYSTEM READY...`), и цикл (прожим → 2 сек ожидания → ошибка) повторяется. Система остаётся в этом цикле до тех пор, пока не будет введена валидная пара позывного и ключа доступа.

    -   **Примечание:** Сообщение об ошибке должно быть универсальным и не раскрывать, какой именно параметр неверен (принцип *security by obscurity*).

## 7. 🧪 Сценарии автоматизированного тестирования (CAS)

> **CAS (Cassandra Automation Scenarios)** — внутренний стандарт именования сценариев автоматизированного тестирования в рамках Cassandra Framework. Формат: `CAS-{NN}: {Краткое описание}`.

- **Структура реализации**
    - Тесты разделены на логические файлы в директории tests/test_login/. Ниже приведено описание каждого сценария с привязкой к файлу реализации.

### test_errors.py — Негативные сценарии и ошибки

- **CAS-01: Неверный пароль для AURORA.**

    -   Переход: Открыть страницу Login.

    -   Действие: Ввести валидный позывной AURORA в поле Callsign и неверный ключ доступа WRONG_CODE в поле Access Code, нажать на кнопку Establish Connection.

    -   Ожидаемый результат: Появляется блок ошибки с текстом ⚠️ Invalid callsign or access code. Текст Telemetry меняется на красный: > SYSTEM FAILURE. INVALID CREDENTIALS. Пользователь остаётся на странице.

- **CAS-02: Cross-user подмена пароля для ORION**

    -   Переход: Открыть страницу Login.

    -   Действие: Ввести валидный позывной ORION в поле Callsign и ключ доступа COMET_42 в поле Access Code от AURORA, нажать на кнопку Establish Connection.

    -   Ожидаемый результат: Появляется блок ошибки с текстом ⚠️ Invalid callsign or access code. Текст Telemetry меняется на красный: > SYSTEM FAILURE. INVALID CREDENTIALS. Пользователь остаётся на странице.

- **CAS-03: Неверный пароль для NOVA**

    -   Предусловие: Создать пользователя NOVA.

    -   Переход: Открыть страницу Login.

    -   Действие: Ввести валидный позывной NOVA в поле Callsign и ключ доступа WRONG_PASS в поле Access Code, нажать на кнопку Establish Connection.

    -   Ожидаемый результат: Появляется блок ошибки с текстом ⚠️ Invalid callsign or access code. Текст Telemetry меняется на красный: > SYSTEM FAILURE. INVALID CREDENTIALS. Пользователь остаётся на странице.

    -   Teardown: Удаляем пользователя NOVA.

- **CAS-04: Попытка входа за Knopa до востановления ключа доступа**

    -   Переход: Открыть страницу Login.

    -   Действие: Ввести валидный позывной KNOPA в поле Callsign и ключ доступа AERO_99 в поле Access Code, нажать на кнопку Establish Connection.

    -   Ожидаемый результат: Система не находит пользователя ни в одной из баз. Ошибка авторизации ⚠️ Invalid callsign or access code, телеметрия красная. Пользователь остаётся на странице.

- **CAS-05: Неверный позывной при валидном ключе**

    -   Переход: Открыть страницу Login.

    -   Действие: Ввести позывной WRONG в поле Callsign и ключ доступа COMET_42 в поле Access Code, нажать на кнопку Establish Connection.

    -   Ожидаемый результат: Появляется блок ошибки с текстом ⚠️ Invalid callsign or access code. Текст Telemetry меняется на красный: > SYSTEM FAILURE. INVALID CREDENTIALS. Пользователь остаётся на странице.

- **CAS-06: Оба поля неверны**

    -   Переход: Открыть страницу Login.

    -   Действие: В поля Callsign и Access Code, ввести невалидные данные WRONG и WRONG_CODE, нажать на кнопку Establish Connection.

    -   Ожидаемый результат: Появляется блок ошибки с текстом ⚠️ Invalid callsign or access code. Текст Telemetry меняется на красный: > SYSTEM FAILURE. INVALID CREDENTIALS. Пользователь остаётся на странице.

### test_success.py — Позитивные сценарии

- **CAS-01: Успешная авторизация пользователя AURORA**

    -   Переход: Открыть страницу Login Page.

    -   Действие: Ввести валидный позывной AURORA и валидный ключ доступа COMET_42, в поля Callsign и Access Code, нажать на кнопку Establish Connection.

    -   Ожидаемый результат: Данные сохраняются в sessionStorage, текст Telemetry меняется на зелёный: > CONNECTION ESTABLISHED. WELCOME, SPECIALIST AURORA. Через 2.0 сек происходит редирект, URL страницы меняется на dashboard.html.

- **CAS-02: Успешная авторизация пользователя ORION**

    -   Переход: Открыть страницу Login Page.

    -   Действие: Ввести валидный позывной ORION и валидный ключ доступа NEBULA_7, в поля Callsign и Access Code, нажать на кнопку Establish Connection.

    -   Ожидаемый результат: Данные сохраняются в sessionStorage, текст Telemetry меняется на зелёный: > CONNECTION ESTABLISHED. WELCOME, COMMANDER ORION. Через 2.0 сек происходит редирект, URL страницы меняется на dashboard.html.

- **CAS-03: Успешная авторизация пользователя NOVA**

    -   Предусловие: Создать пользователя NOVA.

    -   Переход: Открыть страницу Login Page.

    -   Действие: Ввести валидный позывной NOVA и валидный ключ доступа QUASAR_5, в поля Callsign и Access Code, нажать на кнопку Establish Connection.

    -   Ожидаемый результат: Данные сохраняются в sessionStorage, текст Telemetry меняется на зелёный: > CONNECTION ESTABLISHED. WELCOME, ENGINEER NOVA. Через 2.0 сек происходит редирект, URL страницы меняется на dashboard.html.

    -   Teardown: Удаляем пользователя NOVA. 

- **CAS-04: Успешная авторизация пользователя KNOPA**

    -   Предусловие: Восстановить ключ доступа пользователю KNOPA через страницу access-restoration.html.

    -   Переход: Открыть страницу Login Page.

    -   Действие: Ввести валидный позывной KNOPA и валидный ключ доступа AERO_99, в поля Callsign и Access Code, нажать на кнопку Establish Connection.

    -   Ожидаемый результат: Данные сохраняются в sessionStorage, текст Telemetry меняется на зелёный: > CONNECTION ESTABLISHED. WELCOME, PILOT KNOPA. Через 2.0 сек происходит редирект, URL страницы меняется на dashboard.html.

    -   Teardown: Удаляем ключ restoredUsers для пользователя KNOPA. 

### test_ui_navigation.py — UI-состояния, тултипы, hover-эффекты, навигация

- **CAS-01: Состояние страницы при загрузке**

    -   Переход: Открыть страницу Login.

    -   Действие: Оценить начальное состояние элементов без ввода данных.

    -   Ожидаемый результат: Виден подзаголовок: Access to Colonization Assessment System. Поля Callsign и Access Code пустые.  Кнопка Establish Connection неактивна disabled. Элемент Telemetry отображается синим цветом с текстом > SYSTEM READY. AWAITING CONNECTION.

- **CAS-02: Реактивное состояние кнопки Establish Connection**

    -   Переход: Открыть страницу Login.

    -   Действие: Ввести в поле Callsign < 4 символов, в поле Access Code < меньше 4 символов. Ввести в поле Callsign 4 символа, в поле Access Code 4 символа.  

    -   Ожидаемый результат: Кнопка Establish Connection, остаётся disabled, пока длина Callsign < 4 и/или длина Access Code < 4 символов. Кнопка Establish Connection становится активной enabled, только при достижении обоих минимумов.

- **CAS-03: Переключение видимости ключа доступа.**

    -   Переход: Открыть страницу `Login`.

    -   Действие: Ввести любой текст в поле `Access Code`, нажать на кнопку `Toggle Password Button` (иконка глаза), затем нажать повторно.
    
    -   Ожидаемый результат: При первом клике символы в поле становятся видимыми (тип меняется на text), иконка глаза меняется. При повторном клике, символы скрываются (тип возвращается к password).

- **CAS-04: Успешная навигация на страницу `Sign Up`.**

    -   Переход: Открыть страницу `Login`.

    -   Действие: Нажать на угловую кнопку `btn-signup'`.

    -   Ожидаемый результат: Происходит навигация, URL страницы меняется на `signup.html`.

- **CAS-05: Успешная навигация на страницу `Access Restoration`.**

    -   Переход: Открыть страницу `Login`.

    -   Действие: Нажать на угловую кнопку `btn-restore'`.

    -   Ожидаемый результат: Происходит навигация, URL страницы меняется на `access-restoration.html`.

### test_validation.py - Проверка состояний, валидация UI, анимации

- **CAS-01: Позывной короче минимальной длины.**

    -   Переход: Открыть страницу `Login`.

    -   Действие: Ввести 3 символа в поле `Callsign`,  и валидный ключ доступа `COMET_42` в поле `Access Code`, попытаться нажать на кнопку `Establish Connection`.

    -   Ожидаемый результат: Кнопка `Establish Connection` остаётся неактивной `disabled`.

- **CAS-02: Ключ доступа короче минимальной длины.**

    -   Переход: Открыть страницу `Login`.

    -   Действие: Ввести валидный позывной `AURORA` в поле `Callsign` , и 3 символа в поле `Access Code`, попытаться нажать на кнопку `Establish Connection`.

    -   Ожидаемый результат: Кнопка `Establish Connection` остаётся неактивной `disabled`.

- **CAS-03: Пустой позывной при заполненном ключе.**

    -   Переход: Открыть страницу `Login`.

    -   Действие: Оставить поле `Callsign` пустым, ввести валидный ключ доступа `COMET_42` в поле `Access Code`, попытаться нажать на кнопку `Establish Connection`.

    -   Ожидаемый результат: Кнопка `Establish Connection` остаётся неактивной `disabled`.

- **CAS-04: Пустой ключ доступа при заполненном позывном.**

    -   Переход: Открыть страницу `Login`.

    -   Действие: Ввести валидный позывной `AURORA` в поле `Callsign`, оставить поле `Access Code` пустым, попытаться нажать на кнопку `Establish Connection`.

    -   Ожидаемый результат: Кнопка `Establish Connection` остаётся неактивной `disabled`. 

- **CAS-05: Превышение максимальной длины Callsign.**

    -   Переход: Открыть страницу `Login`.

    -   Действие: Попытаться ввести >100 символов в поле `Callsign`.

    -   Ожидаемый результат: Ввод блокируется на уровне UI, в поле `Callsign`, остаётся не более 100 символов.

- **CAS-06: Превышение максимальной длины Access Code.**

    -   Переход: Открыть страницу `Login`.

    -   Действие: Попытаться ввести >30 символов в поле `Access Code`.

    -   Ожидаемый результат: Ввод блокируется на уровне UI, в поле остаётся только первые 30 валидных символов.

- **CAS-07: Санитизация ввода — попытка ввести спецсимволы в поле Callsign.**

    -   Переход: Открыть страницу `Login`.

    -   Действие: В поле Callsign ввести строку со спецсимволами `' OR '1'='1`. В поле `Access Code` ввести валидный ключ доступа. Нажать на кнопку `Establish Connection`.

    -   Ожидаемый результат: Фронтенд автоматически отсекает спецсимволы (', пробелы) — в поле остаётся только допустимая часть `OR11`. После нажатия кнопки появляется стандартная ошибка валидации `⚠️ Invalid callsign or access code`. Данные не сохраняются в `sessionStorage`.

## 8. 🎨 Визуальные требования

-   **Цветовая схема:** Тёмно-синий космический фон '#050816', голубые акценты '#4da6ff', белые текстовые элементы.

-   **Анимации:** 

    -   Мигающий курсор в строке `Telemetry` (1s, step-end).

    -   Пульсация радиоволн спутника (2s цикл, 3 волны с задержкой).

    -   Свечение кнопки `Establish Connection` при наведении. Эффект применяется **только** когда кнопка активна (введено ≥ 4 символов в `Callsign` и ≥ 4 в `Access Code`). В состоянии `disabled` кнопка выглядит тусклой и не имеет эффектов при наведении.

-   **Карточка формы:** Полупрозрачный фон с `backdrop-filter: blur`, голубая рамка со свечением.

-   **Адаптивность:** `Desktop-only` (1024px+). Мобильная версия не предусмотрена.

## 9. 🏷️ Спецификация data-wm-id (Ironclad Locators)

> Все интерактивные элементы должны иметь уникальный атрибут `data-wm-id` для надёжной автоматизации.

| Элемент | data-wm-id |
|---------|-----------|
| Подзаголовок страницы | `login-page-subtitle`|
| Поле Callsign | `login-callsign-input`|
| Поле Access Code | `login-access-code-input`|
| Кнопка Establish Connection | `establish-connect-btn`|
| Блок ошибки авторизации | `auth-error-message`|
| Строка телеметрии | `system-telemetry`|
| Кнопка переключения видимости пароля | `toggle-password-btn`|
| Угловая кнопка регистрации (Sign Up) | `btn-signup`|
| Угловая кнопка восстановления (Restore) | `btn-restore`|

## 10. 🌐 URL и маршрутизация

Так как проект является учебным и статическим (без серверной части), для локальной разработки и деплоя на GitHub Pages используются следующие адреса:

- **Local Base URL (Локальная разработка и тесты):** 
  - Главная: `file:///.../cassandra/index.html`
  - Авторизация: `file:///.../cassandra/login.html`
  - Регистрация: `file:///.../cassandra/signup.html`
  - Восстановление: `file:///.../cassandra/access-restoration.html`

- **GitHub Pages URL (Публичный деплой):** 
  - Главная: `https://<username>.github.io/cassandra/index.html`
  - Авторизация: `https://<username>.github.io/cassandra/login.html`
  - Регистрация: `https://<username>.github.io/cassandra/signup.html`
  - Восстановление: `https://<username>.github.io/cassandra/access-restoration.html`

  *(Остальные страницы формируются аналогично, с обязательным суффиксом `.html`)*

> **Примечание для автоматизации:** 
> В конфигурационных файлах фреймворка (например, `config.py`) определяется базовый URL (`BASE_URL`) в зависимости от окружения. 
> Переход на конкретную страницу осуществляется путем добавления имени файла (например, `login.html`) к `BASE_URL`. Это гарантирует корректную работу тестов как на локальной машине, так и в CI/CD.

*Версия ТЗ: 1.5 | Обновлено: 08.09.2026 | Автор: Evknopia*