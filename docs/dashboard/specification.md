# 🚀 Cassandra Framework
## Техническое задание: Страница Dashboard (Загрузка системы)

### 📌 Обозначения

-   **`ElementName`** *(type)* — UI-элемент с указанием типа
-   *"Text content"* — отображаемый текст на интерфейсе
-   `code` — технические термины, классы, методы
-   `'/dashboard.html'` *(url)* — адрес страницы (URL) для проверки навигации
-   ⚠️ *(decorative)* — декоративный элемент, не подлежит автоматизированному тестированию

## 1. 🎯 Назначение
Центральный экран управления системой оценки колонизации. Обеспечивает отображение данных авторизованного пользователя, последовательную визуальную инициализацию (загрузку) модулей системы по команде `Uplink`, навигацию по основным разделам и безопасный разрыв сессии (выход из системы).

## 2. 🖥️ Элементы интерфейса

### 2.1 Информационные панели (Info Circles)

- **`Role Panel`**  *(info-circle)* — Панель роли пользователя (верхний левый угол). Содержит:

    - **`Role Icon`** *(emoji)* — Иконка роли.

    - **`Role Label`** *(text)* — Название роли.

    - **`Role Tooltip`** *(tooltip)* — Всплывающая подсказка при наведении.

- **`User Panel`** *(info-circle)* — Панель пользователя (верхний правый угол). Содержит:

    - **`User Icon`** *(emoji)* — Иконка астронавта.

    - **`User Label`** *(text)* — Позывной пользователя.

    - **`Status Dot`** *(decorative)* — Индикатор статуса.

    - **`User Tooltip`** *(tooltip)* — Всплывающая подсказка.

### 2.2 Декоративные элементы ⚠️

> **Примечание:** Элементы данного раздела являются частью визуального оформления страницы и **не подлежат автоматизированному тестированию**. Они не имеют `data-wm-id` и не включаются в CAS-сценарии.

-   **`Cassandra Logo`** ⚠️ *(decorative/text)* — Заголовок "CASSANDRA" в центре.

-   **`Stars Background`** *(decorative/css)* — Анимированный фон со звёздами и Млечным Путём.

### 2.3 Основные элементы управления

- **`Uplink Button`** (button) — Кнопка инициализации системы (нижний правый угол). Label: `"Uplink"`. Иконка: `📡`.
Состояние по умолчанию: Активна, имеет анимацию пульсации (uplink-active).
Состояние после 100% загрузки: Неактивна, теряет пульсацию, становится серой (grayscale) и полупрозрачной (opacity: 0.5).

- **`Logout Button`** (button) — Кнопка разрыва связи / выхода из системы (нижний левый угол). Label: `"Logout"`. Иконка: `🌑`. Изначально скрыта (uplink-hidden).
Поведение при наведении: В состоянии panel-online кнопка увеличивается (scale(1.15)) и усиливает неоновое свечение — симметрично с центральными навигационными кнопками.

- `Planet Bar` (button-group) — Контейнер с 5 центральными навигационными кнопками. Изначально скрыты (uplink-hidden). Все кнопки имеют эффект увеличения при наведении в активном состоянии:

    - **`Galaxy Map Btn`** (button) — Label: `"Galaxy Map"`. Иконка: `🌌`.

    - **`Flight Calc Btn`** (button) — Label: `"FLIGHT CALC"`. Иконка: `💫`.

    - **`CIS Table Btn`** (button) — Label: `"CIS INDEX TABLE"`. Иконка: `🧬`.

    - **`Mission Control Btn`** (button) — Label: `"MISSION CONTROL"`. Иконка: `🚀`.

    - **`Settings Btn`** (button) — Label: `"Cassandra Settings"`. Иконка: `⚙️`.

### 2.4 Служебные элементы

- **`Uplink Progress Container`** (decorative/container) — Контейнер прогресс-бара загрузки. Изначально скрыт. Содержит Progress Track, Progress Fill и Progress Text.

-   **`Telemetry`** *(text)* — Строка состояния системы в футере. Отображается моноширинным шрифтом с мигающим курсором (`█`). 

    - **Важно** (Tone of Voice): Весь текст выводится от лица системного ИИ CASSANDRA и всегда начинается с префикса > CASSANDRA: (в коде стилизован как > CASSAN #ffffff + DRA #4da6ff).

    -   Состояние по умолчанию (до uplink): *"> {FULL_NAME}, SERVER CONNECTION REQUIRED. INITIALIZE UPLINK PROCEDURE."* (синий цвет, rgb(77, 166, 255) / #4da6ff).

    -   Состояние после (после uplink): *"> {FULL_NAME}, SYSTEM READY FOR WORK. AWAITING COMMANDS."* (зелёный цвет,rgb(46, 204, 113) / #2ecc71).

## 3. ✅ Правила состояний (State Rules)

- **Состояние `panel-offline`** 

    - Применяется к информационным панелям при загрузке, а также к кнопкам (Logout, Planet Bar) в момент их появления во время загрузки.

    - Визуально: Элементы выглядят цветными, с неоновым свечением (не серые и не тусклые).

    - Интерактивно: Полностью заблокированы для мыши (pointer-events: none, cursor: default).

    - Тултипы скрыты (display: none), клики и наведения (hover) не срабатывают.

- **Состояние `panel-online`**

    - Применяется ко всем вышеуказанным элементам после достижения прогресса 100%. Элементы приобретают полную интерактивность (pointer-events: auto), тултипы становятся доступны, срабатывают hover-эффекты.

    - Состояние uplink-hidden / uplink-visible: Управляет видимостью центральных и нижних кнопок. Переход сопровождается пружинистой анимацией (cubic-bezier).

## 4. ⚙️ Логика работы

> ⚠️ **Важно: Контекст проекта**
> Проект не имеет серверной части. Данные пользователя читаются исключительно из sessionStorage (ключ currentUser).

- **Инициализация `initDashboard`**

    - При загрузке DOM скрипт проверяет наличие currentUser в sessionStorage.

    - Если данных нет или они повреждены: текст Telemetry становится красным, через 2.0 сек происходит редирект на login.html.

    - Если данные валидны: скрипт динамически подставляет данные в Role Panel и User Panel. Запускается эффект печатной машинки в Telemetry.

- **Процедура Uplink `startUplink`**

    - По клику на Uplink Button кнопка деактивируется. Появляется Uplink Progress Container, запускается асинхронная последовательность:

        - `10%`:Появляется `Logout Button` (в состоянии panel-offline).

        - `22%`: Текст в телеметрии: `"LOADING CENTRAL MENU CONTROL"`.

        - `36% - 95%`: Поэтапное появление кнопок `Planet Bar` (в состоянии panel-offline).

        - `99%`: Текст в телеметрии: `"SYNCHRONIZING WITH CORE"`.

        - `100%`:Все кнопки (Logout, Planet Bar) и панели переключаются из panel-offline в panel-online.
        Кнопка Uplink Button теряет класс пульсации, становится серой (grayscale(100%)) и полупрозрачно (opacity: 0.5).Прогресс-бар плавно исчезает.
        
        - Текст телеметрии становится зеленым: `"{CALLSIGN}, SYSTEM READY FOR WORK. AWAITING COMMANDS."`

    - **Процедура выхода `handleLogout`**

        - По клику на Logout Button вызывается функция handleLogout().

        - Из sessionStorage удаляется ключ currentUser.

        - Происходит мгновенный редирект на login.html.

## 5. 🤖 Спецификация данных: Объект currentUser

- **Источник данных:** `sessionStorage.getItem` ('currentUser').
Структура данных (валидный JSON): Содержит поля callsign, fullName, role, roleIcon, function, accessLevel, id.

- {
    - "callsign": "AURORA",
    - "role": "Specialist",
    - "fullName": "Aurora",
    - "roleIcon": "🛰️",
    - "function": "Comms & Diagnostics",
    - "accessLevel": "2",
    - "id": "884-2A"
- }

 >⚠️ Важные примечания о специфических операторах:

- **`Оператор NOVA:`**

- Происхождение: Создается исключительно через процедуру регистрации на странице `signup.html`.

- **Хранение:** После успешной регистрации его полные данные сохраняются в `localStorage` по ключу `registeredUsers`. При входе в систему эти данные считываются и помещаются в `sessionStorage` (currentUser).

    - {
        - "callsign": "NOVA",
        - "fullName": "Nova",
        - "role": "ENGINEER",
        - "roleIcon": "⚙️",
        - "function": "Systems Engineering",
        - "accessLevel": "3",
        - "id": "512-3A"
    - }


- **Оператор KNOPA:**

- Происхождение: Является предсуществующим, но не имеет записи в `registeredUsers`. Попадает в систему исключительно через процедуру восстановления на странице `access-restoration.html`.

- **Хранение:** После успешного восстановления его данные сохраняются в `localStorage` по ключу `restoredUsers` (с обновленным `accessCode: "AERO_99"`). При входе в систему данные считываются оттуда и помещаются в `sessionStorage` (currentUser).

    - {
        - "callsign": "KNOPA",
        - "fullName": "Knopa",
        - "role": "PILOT",
        - "roleIcon": "✈️",
        - "function": "Flight Operations",
        - "accessLevel": "1",
        - "id": "769-1A"
    - }

## 6. 🧪 Сценарии автоматизированного тестирования (CAS)

> **CAS (Cassandra Automation Scenarios)** — внутренний стандарт именования сценариев автоматизированного тестирования в рамках Cassandra Framework. Формат: `CAS-{NN}: {Краткое описание}`.

- **Структура реализации**
    - Тесты разделены на логические файлы в директории tests/test_dashboard/. Ниже приведено описание каждого сценария с привязкой к файлу реализации.

### test_errors.py — Негативные сценарии и ошибки

- **CAS-01: Редирект при отсутствии данных в sessionStorage.**

    - Переход: Открыть страницу Dashboard Page.

    - Действие: Оценить реакцию системы при загрузке.

    - Ожидаемый результат: Текст телеметрии становится красным и содержит > ACCESS DENIED. REDIRECTING.... Через 2.0 сек происходит автоматический редирект на login.html.

- **CAS-02: Обработка поврежденных данных в sessionStorage**

    - Переход: Открыть страницу Dashboard Page.

    - Действие: Вручную установить в sessionStorage ключ currentUser со значением невалидной JSON-строки (например, broken). Обновить страницу.

    - Ожидаемый результат: Текст телеметрии становится красным и содержит > DATA CORRUPTED. REDIRECTING.... Через 2.0 сек ключ currentUser удаляется из sessionStorage, происходит редирект на login.html.

- **CAS-03: Защита от восстановления сессии через кнопку Назад после Logout**

    - Переход: Выполнить авторизацию под пользователем AURORA. Открыть страницу Dashboard Page.

    - Действие: Нажать на кнопку Uplink, дождаться 100% загрузки. Нажать на кнопку Logout, дождаться редиректа на login.html. Нажать кнопку Назад в браузере.

    - Ожидаемый результат:Пользователь остаётся на странице login.html. Ключ currentUser в sessionStorage отсутствует (null).

- **CAS-04: Защита от спама кликов по кнопке Uplink**

    - Переход: Выполнить авторизацию под пользователем AURORA. Открыть страницу Dashboard Page.

    - Действие: Выполнить 3 последовательных клика по кнопке Uplink.

    - Ожидаемый результат: Физически проходит только 1 клик, последующие попытки прерываются, так как кнопка блокируется интерфейсом. Анимация загрузки (прогресс-бар) появляется ровно 1 раз. Кнопка корректно переходит в неактивное состояние.

### test_interactions.py — Взаимодействие с элементами интерфейса

- **CAS-01: Динамическая подстановка данных в тултипы для 'AURORA'**

    - Переход:Выполнить авторизацию под пользователем 'AURORA'. Открыть страницу Dashboard Page.

    - Действие: Нажать на кнопку Uplink и дождаться 100% загрузки. Поочередно навести курсор мыши на панели Role Panel и User Panel.

    - Ожидаемый результат: Тултип Role Panel отображает текст Access: Level 2. Тултип User Panel отображает текст ID: 884-2A.

- **CAS-02: Динамическая подстановка данных в тултипы для 'ORION'**

    - Переход: Выполнить авторизацию под пользователем 'ORION'. Открыть страницу Dashboard Page.

    - Действие: Нажать на кнопку Uplink и дождаться 100% загрузки. Поочередно навести курсор мыши на панели Role Panel и User Panel.

    - Ожидаемый результат: Тултип Role Panel отображает текст Access: Level 1. Тултип User Panel отображает текст ID: 001-1A.

- **CAS-03: Динамическая подстановка данных в тултипы для 'NOVA'**

    - Предусловие: Создать пользователя 'NOVA'

    - Переход: Выполнить авторизацию под пользователем 'NOVA'. Открыть страницу Dashboard Page.

    Действие: Нажать на кнопку Uplink и дождаться 100% загрузки. Поочередно навести курсор мыши на панели Role Panel и User Panel.

    - Ожидаемый результат: Тултип Role Panel отображает текст Access: Level 3. Тултип User Panel отображает текст ID: 512-3A.
Teardown: Удалить пользователя 'NOVA'.

    - Teardown: Удалить пользователя 'NOVA'. 

- **CAS-04: Динамическая подстановка данных в тултипы для 'KNOPA'** 

    - Предусловие: Восстановить доступ пользователю 'KNOPA' через страницу access-restoration.html.

    - Переход: Выполнить авторизацию под пользователем 'KNOPA'. Открыть страницу Dashboard Page.

    - Действие: Нажать на кнопку Uplink и дождаться 100% загрузки. Поочередно навести курсор мыши на панели Role Panel и User Panel.

    - Ожидаемый результат: Тултип Role Panel отображает текст Access: Level 1. Тултип User Panel отображает текст ID: 769-1A.

    - Teardown: Удалить ключ restoredUsers для пользователя 'KNOPA'.

- **CAS-05: Hover-эффект кнопки Logout в активном состоянии**

    - Переход: Выполнить авторизацию под пользователем 'AURORA'. Открыть страницу Dashboard Page.

    - Действие: Нажать на кнопку Uplink и дождаться 100% загрузки. Навести курсор мыши на кнопку Logout.

    - Ожидаемый результат: Кнопка увеличивается в размере (transform: scale(1.15)), рамка и свечение становятся ярче. Курсор меняется на pointer.

- **CAS-06: Пост-активационная навигация по 'Planet Bar'**

    - Переход: Выполнить авторизацию под пользователем 'AURORA'. Открыть страницу 'Dashboard Page'.

    - Действие: Нажать на кнопку 'Uplink' и дождаться 100% загрузки. Поочередно нажать на доступные кнопки 'Planet Bar' (исключая 'Flight Calc Btn').

    - Ожидаемый результат: Каждое нажатие приводит к успешному переходу браузера на соответствующий URL (galaxy-map.html, cis-table.html и т.д.). Кнопка 'Flight Calc' игнорируется до реализации компонента-драйвера.

- **CAS-07: Восстановление состояния дашборда после возврата с другой страницы**

    - Переход: Выполнить авторизацию под пользователем 'AURORA'. Открыть страницу 'Dashboard Page'.

    - Действие: Нажать на кнопку 'Uplink' и дождаться 100% загрузки. Нажать на кнопку 'Galaxy Map Btn', дождаться перехода на galaxy-map.html. Нажать на кнопку 'Дашборд', чтобы вернуться назад.

    - Ожидаемый результат: Все элементы (2 верхние панели + 6 кнопок) находятся в состоянии panel-online. Текст телеметрии содержит зелёную фразу > CASSANDRA: AURORA, SYSTEM READY FOR WORK. AWAITING COMMANDS. Кнопка 'Uplink' остаётся неактивной (полупрозрачной, без пульсации). sessionStorage содержит валидные данные пользователя.

### test_states.py — Валидация состояний UI

- **CAS-01: Успешная инициализация панели для 'AURORA'**

    - Переход: Выполнить авторизацию под пользователем 'AURORA'. Открыть страницу 'Dashboard Page'.

    - Действие: Оценить содержимое панелей 'Role Panel' и 'User Panel' до нажатия на кнопку 'Uplink'.

    - Ожидаемый результат: Текст 'Role Label' содержит 'SPECIALIST', текст 'User Label' содержит 'AURORA'. Элементы имеют класс panel-offline, некликабельны, тултипы скрыты.

- **CAS-02: Успешная инициализация панели для 'ORION'**

    - Переход: Выполнить авторизацию под пользователем 'ORION'. Открыть страницу 'Dashboard Page'.

    - Действие: Оценить содержимое панелей 'Role Panel' и 'User Panel' до нажатия на кнопку 'Uplink'.

    - Ожидаемый результат: Текст 'Role Label' содержит 'COMMANDER', текст 'User Label' содержит 'ORION'. Элементы имеют класс panel-offline, некликабельны, тултипы скрыты.

- **CAS-03: Успешная инициализация панели для 'NOVA'**

    - Предусловие: Создать пользователя 'NOVA'.

    - Переход: Выполнить авторизацию под пользователем 'NOVA'. Открыть страницу 'Dashboard Page'.

    - Действие: Оценить содержимое панелей 'Role Panel' и 'User Panel' до нажатия на кнопку 'Uplink'.

    - Ожидаемый результат: Текст 'Role Label' содержит 'ENGINEER', текст 'User Label' содержит 'NOVA'. Элементы имеют класс panel-offline, некликабельны, тултипы скрыты.

    - Teardown: Удалить пользователя 'NOVA'.

- **CAS-04: Успешная инициализация панели для 'KNOPA'** 

    - Предусловие: Восстановить доступ пользователю 'KNOPA' через страницу access-restoration.html.

    - Переход: Выполнить авторизацию под пользователем 'KNOPA'. Открыть страницу 'Dashboard Page'.

    - Действие: Оценить содержимое панелей 'Role Panel' и 'User Panel' до нажатия на кнопку 'Uplink'.

    - Ожидаемый результат: Текст 'Role Label' содержит 'PILOT', текст 'User Label' содержит 'KNOPA'. Элементы имеют класс panel-offline, некликабельны, тултипы скрыты.

    - Teardown: Удалить ключ restoredUsers для пользователя 'KNOPA'.

- **CAS-05: Проверка неактивности кнопки 'Logout' до полной активации**

    - Переход: Выполнить авторизацию под пользователем 'AURORA'. Открыть страницу 'Dashboard Page'.

    - Действие: Нажать на кнопку 'Uplink'. Дождаться появления кнопки 'Logout' на 10% загрузки прогресс-бара. После появления кнопки попытаться нажать на нее и проверить URL.

    - Ожидаемый результат: Переход на другую страницу не происходит. Кнопка 'Logout' имеет CSS-свойство pointer-events: none, игнорирующее взаимодействия.

- **CAS-06: Проверка неактивности кнопок 'Planet Bar' до полной активации**

    - Переход: Выполнить авторизацию под пользователем 'AURORA'. Открыть страницу 'Dashboard Page'.

    - Действие: Нажать на кнопку 'Uplink'. Дождаться поочередного появления 5 кнопок 'Planet Bar', начиная с 36% загрузки прогресс-бара. После появления каждой новой кнопки попытаться нажать на нее и проверить URL.

    - Ожидаемый результат: Переход на другие страницы не происходит. Все кнопки 'Planet Bar' имеют CSS-свойство pointer-events: none, игнорирующее взаимодействия.

- **CAS-07: Валидация анимации и значений прогресс-бара**

    - Переход: Выполнить авторизацию под пользователем 'AURORA'. Открыть страницу 'Dashboard Page'.

    - Действие: Нажать на кнопку 'Uplink'. Через явные ожидания проверять значение атрибута style (width) у элемента Progress Fill и текст у Progress Text.

    - Ожидаемый результат: Значение ширины монотонно возрастает (10% -> 22% -> ... -> 100%). Текстовое значение синхронизировано с шириной. В конце анимации контейнер прогресс-бара получает display: none или opacity: 0.

- **CAS-08: Визуальное угасание кнопки 'Uplink' после 100% загрузки**

    - Переход: Выполнить авторизацию под пользователем 'AURORA'. Открыть страницу 'Dashboard Page'.

    - Действие: Нажать на кнопку 'Uplink' и дождаться 100% загрузки. Проверить CSS-свойства кнопки 'Uplink'.

    - Ожидаемый результат: У кнопки отсутствует класс uplink-active, пульсация остановлена. Кнопка приобретает класс uplink-inactive, свойство opacity равно 0.5, кликабельность отключена (pointer-events: none).
    
### test_workflows.py - Сквозные бизнес-сценарии

- **CAS-01: Полная последовательность активации 'Uplink'**

    - Переход: Выполнить авторизацию под пользователем 'AURORA'. Открыть страницу 'Dashboard Page'.

    - Действие: Нажать на кнопку 'Uplink' и дождаться завершения анимации (100%).

    - Ожидаемый результат: Кнопка 'Uplink' становится неактивной, теряет пульсацию и становится тусклой. Прогресс-бар последовательно заполняется, кнопки появляются поэтапно. Все 6 кнопок ('Logout' и 5 центральных), а также панели 'Role Panel' и 'User Panel', в конце приобретают класс panel-online, становятся яркими, с неоновым свечением, кликабельными и визуально увеличиваются при наведении курсора. Текст телеметрии становится зеленым и содержит фразу > SYSTEM READY FOR WORK.

- **CAS-02: Успешный выход из системы для 'AURORA'**

    - Переход: Выполнить авторизацию под пользователем 'AURORA'. Открыть страницу 'Dashboard Page'.

    - Действие: Нажать на кнопку 'Uplink' и дождаться 100% загрузки. Нажать на кнопку 'Logout'.

    - Ожидаемый результат: Значение currentUser в sessionStorage становится null. URL браузера меняется на login.html.

- **CAS-03: Успешный выход из системы для 'ORION'**

    - Переход: Выполнить авторизацию под пользователем 'ORION'. Открыть страницу 'Dashboard Page'.

    - Действие: Нажать на кнопку 'Uplink' и дождаться 100% загрузки. Нажать на кнопку 'Logout'.

    - Ожидаемый результат: Значение currentUser в sessionStorage становится null. URL браузера меняется на login.html.

- **CAS-04: Успешный выход из системы для 'NOVA'**

    - Предусловие: Создать пользователя 'NOVA'.

    - Переход: Выполнить авторизацию под пользователем 'NOVA'. Открыть страницу 'Dashboard Page'.

   - Действие: Нажать на кнопку 'Uplink' и дождаться 100% загрузки. Нажать на кнопку 'Logout'.

   - Ожидаемый результат: Значение currentUser в sessionStorage становится null. URL браузера меняется на login.html. Значение registeredUsers в localStorage сохраняется.

    - Teardown: Удалить пользователя 'NOVA'.

- **CAS-05: Успешный выход из системы для 'KNOPA'** 

    - Предусловие: Восстановить доступ пользователю 'KNOPA' через страницу access-restoration.html.

    - Переход: Выполнить авторизацию под пользователем 'KNOPA'. Открыть страницу 'Dashboard Page'.

    - Действие: Нажать на кнопку 'Uplink' и дождаться 100% загрузки. Нажать на кнопку 'Logout'.

    - Ожидаемый результат: Значение currentUser в sessionStorage становится null. URL браузера меняется на login.html. Значение restoredUsers в localStorage сохраняется.

    - Teardown: Удалить ключ restoredUsers для пользователя 'KNOPA'.

- **CAS-06: Корректная переинициализация состояния при восстановлении страницы из bfcache** 

    - Переход: Выполнить авторизацию под пользователем 'AURORA'. Открыть страницу 'Dashboard Page'. Нажать на кнопку 'Uplink' и дождаться 100% загрузки.

    - Действие: Нажать на кнопку 'Galaxy Map', дождаться перехода на страницу galaxy-map.html, затем нажать кнопку «Назад» в браузере.

    - Ожидаемый результат: Ключ currentUser присутствует в sessionStorage. Кнопки 'Planet Bar' активны (имеют класс panel-online). Текст телеметрии содержит SYSTEM READY FOR WORK.

## 7. 🎨 Визуальные требования

- **Цветовая схема:** Тёмно-синий космический фон #050816, голубые акценты #4da6ff, зеленый статус #2ecc71.

-   **Анимации:** 

    - Пульсация `Uplink Button` до нажатия `uplink-pulse`.

    - Плавное (пружинистое) появление кнопок (`cubic-bezier`(0.175, 0.885, 0.32, 1.275)).

    - Увеличение кнопок `Planet Bar` и `Logout Button` при наведении в активном состоянии (`scale`(1.15)).

    - Мигающий курсор в `Telemetry` (1s, step-end).

- **Адаптивность:**  

    - Адаптивность: `Desktop-only` (1024px+). Мобильная версия не предусмотрена.

## 8. 🏷️ Спецификация data-wm-id (Ironclad Locators)

> Все интерактивные элементы должны иметь уникальный атрибут `data-wm-id` для надёжной автоматизации.

| Элемент | data-wm-id |
|---------|-----------|
| Панель роли | `info-panel-role`|
| Иконка роли | `role-icon`|
| Текст роли| `role-label`|
| Панель пользователя| `info-panel-user`|
| Иконка пользователя| `user-icon`|
| Текст пользователя| `user-label`|
| Индикатор статуса| `status-dot`|
| Кнопка Uplink| `uplink-btn`|
| Кнопка Logout| `logout-btn`|
| Кнопка Galaxy Map| `nav-galaxy-map`|
| Кнопка Flight Calc| `flight-calc-btn`|
| Кнопка CIS Index Table| `cis-table-btn`|
| Кнопка Mission Control| `mission-control-btn`|
| Кнопка Cassandra Settings| `nav-settings`|
| Строка телеметрии| `system-telemetry`|

 ## 9. 🌐 URL и маршрутизация
Так как проект является учебным и статическим (без серверной части), для локальной разработки и деплоя на GitHub Pages используются следующие адреса:

- **Local Base URL (Локальная разработка и тесты):** 
  - Главная: `file:///.../cassandra/index.html`
  - Дашборд: `file:///.../cassandra/dashboard.html`

- **GitHub Pages URL (Публичный деплой):** 
  - Главная: `https://<username>.github.io/cassandra/`
  - Дашборд: `https://<username>.github.io/cassandra/dashboard.html`

- *(Остальные страницы формируются аналогично, с обязательным суффиксом `.html`)*

> **Примечание для автоматизации:** 
> В конфигурационных файлах фреймворка (например, `config.py`) определяется базовый URL (`BASE_URL`) в зависимости от окружения. 
> Переход на конкретную страницу осуществляется путем добавления имени файла (например, `login.html`) к `BASE_URL`. Это гарантирует корректную работу тестов как на локальной машине, так и в CI/CD.

*Версия ТЗ: 2.2 (Production Ready) | Обновлено: 26.08.2026 | Автор: Evknopia*